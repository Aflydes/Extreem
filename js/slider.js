window.onload = function() {

    const swiper = new Swiper('.swiper-container', {
        // Optional parameters
        direction: 'vertical',
        loop: false,
        pagination: {
            el: '.swiper-pagination',
          },
      
      });
}